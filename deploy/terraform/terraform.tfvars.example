# Copy to terraform.tfvars and fill in. Do NOT commit terraform.tfvars.
# Secrets can also be supplied via environment, e.g. TF_VAR_sql_admin_password=...

prefix   = "hemedical"
location = "westeurope"

sql_admin_login    = "hemedicaladmin"
sql_admin_password = "CHANGE-ME-Strong!Passw0rd"

# fhir.loinc.org account — needed only for custom (non-preset) LOINC codes.
loinc_username = ""
loinc_password = ""
