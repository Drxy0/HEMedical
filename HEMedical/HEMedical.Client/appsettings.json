{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "HEServerBaseUrl": "https://localhost:5010",
  "PlainServerBaseUrl": "https://localhost:5020",
  "//Admin": "Shared secret the Client presents to the HE Server admin API. Must match HE Server's Admin:Secret. Override outside source control in production.",
  "Admin": {
    "Secret": "dev-only-admin-secret-change-me"
  },
  "//Auth": "Placeholder credentials + JWT signing key for the admin dashboard; replace with a real identity provider (e.g. Firebase).",
  "Auth": {
    "JwtSecret": "dev-only-insecure-signing-secret-change-me-32b"
  },
  "Loinc": {
    "BaseUrl": "https://fhir.loinc.org/"
  },
  "Breakdown": {
    "MaxBuckets": 50,
    "MaxConcurrency": 4
  }
}
