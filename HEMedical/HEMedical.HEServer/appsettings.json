{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "//Admin": "Shared secret required (X-Admin-Secret header) for the hospital admin API. Must match the Client's Admin:Secret. Override outside source control in production.",
  "Admin": {
    "Secret": "dev-only-admin-secret-change-me"
  }
}
